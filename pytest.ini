[pytest]
# Skip the example_code tree so its third-party samples don't run with the main suite
addopts = --ignore=example_code
# Extend default ignore list to silence Hypothesis warning and skip vendor dirs
norecursedirs = .* build dist example_code .venv .env .hypothesis
