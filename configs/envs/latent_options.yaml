seed: 123
max_steps: 8

reward:
  success_threshold: 0.05
  success_bonus: 1.0
  step_penalty: 0.05
  invalid_penalty: 0.1
  distance_scale: 1.0
  metric: "cosine"

options:
  include_defaults: true
  extra:
    - name: recolor_to_background
      primitive: recolor
      params:
        source: 1
        target: 0

task_schedule:
  atomic: 4
  sequential: 2
